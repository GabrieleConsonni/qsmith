info:
  name: authentication
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/mock/vulki/authenticator/ws/rest/public/v1/authentication"
  headers:
    - name: akn-device-id
      value: TECH
  auth:
    type: basic
    username: bruno
    password: password

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
